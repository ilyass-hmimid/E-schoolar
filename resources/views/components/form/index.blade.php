@props([
    // This component doesn't render anything itself
    // It's just a way to include all form components at once
])

{{-- This file is intentionally left blank --}}
{{-- It's used as a way to include all form components with a single @include --}}
